<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.aojiaoo.modules.sys.mapper.TestMapper">

    <resultMap id="BaseResultMap" type="com.aojiaoo.modules.sys.entity.Test">
                <id column="id" jdbcType="CHAR" property="id"/>
                <result column="name" jdbcType="VARCHAR" property="name"/>
    </resultMap>

    <sql id="Base_Column_List">
            id,name
    </sql>


    <select id="selectByPrimaryKey" resultMap="BaseResultMap">
        select
        <include refid="Base_Column_List"/>
        from a_test
        <where>
              and  id = #{id,jdbcType=CHAR}
        </where>
    </select>

    <select id="selectBySelective" resultMap="BaseResultMap">
        select
        <include refid="Base_Column_List"/>
        from a_test a
        <where>
               <if test="id != null">
                   and a.id = #{id,jdbcType=CHAR}
               </if>
               <if test="name != null">
                   and a.name = #{name,jdbcType=VARCHAR}
               </if>
        </where>
    </select>



    <insert id="insert" parameterType="com.aojiaoo.modules.sys.entity.Test">
        insert into a_test (
            id,name
        )
        values (
            #{id,jdbcType=CHAR},
            #{name,jdbcType=VARCHAR}
        )
    </insert>


    <insert id="insertSelective" parameterType="com.aojiaoo.modules.sys.entity.Test">

        insert into a_test
        <trim prefix="(" suffix=")" suffixOverrides=",">
                    <if test="id != null">
                        id,
                    </if>
                    <if test="name != null">
                        name,
                    </if>
        </trim>
        <trim prefix="values (" suffix=")" suffixOverrides=",">
                   <if test="id != null">
                       #{id,jdbcType=CHAR},
                   </if>
                   <if test="name != null">
                       #{name,jdbcType=VARCHAR},
                   </if>
        </trim>
    </insert>

    <update id="updateByPrimaryKeySelective" parameterType="com.aojiaoo.modules.sys.entity.Test">
        update a_test
        <set>
                 <if test="name != null">
                     name = #{name,jdbcType=VARCHAR},
                 </if>
        </set>

        <where>
              and  id = #{id,jdbcType=CHAR}
        </where>
    </update>

    <update id="updateByPrimaryKey" parameterType="com.aojiaoo.modules.sys.entity.Test">
        update a_test
        <set>
                   name = #{name,jdbcType=VARCHAR},
        </set>

        <where>
              and  id = #{id,jdbcType=CHAR}
        </where>
    </update>

    <delete id="deleteByPrimaryKey">
        delete from a_test
        <where>
              and  id = #{id,jdbcType=CHAR}
        </where>
    </delete>

</mapper>